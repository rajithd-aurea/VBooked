@{
    ViewBag.Title = "ForApproval";
    Layout = "~/Views/Admin/AdminPageTemplate.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            For Approval
        </h1>
        <ol class="breadcrumb">
            <li>
                Venues
            </li>
            <li>
                For Approval
            </li>
        </ol>

        <div class="table-responsive">
            <table class="table table-bordered table-hover table-striped">
                <thead>
                    <tr>
                        <th>Venue Name</th>
                        <th>Option</th>
                    </tr>
                </thead>
                <tbody id="unapprovedVenueList">
                    @{
                        var venuelist = ViewBag.VenueList;

                        foreach (var venue in venuelist)
                        {
                            <tr>
                                <td><p>@venue.VenueInfo.Name</p></td>
                                <td>
                                    @Html.ActionLink("Approve", "Approve", "Venue", new { venueid = venue.VenueInfo.Pk_VenueId, email = venue.User.Email, venuename = venue.VenueInfo.Name }, null) |
                                    @Html.ActionLink("Deny", "", "", new { venueid = venue.VenueInfo.Pk_VenueId, email = venue.User.Email, venuename = venue.VenueInfo.Name }, null) |
                                    @Html.ActionLink("Suspend", "", "", new { venueid = venue.VenueInfo.Pk_VenueId, email = venue.User.Email, venuename = venue.VenueInfo.Name }, null)
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>